#include<stdio.h>
int main(){
    float p, t, r;
    printf("Enter Principle amount, time and rate: ");
    scanf("%f %f %f", &p, &t, &r);
    SI = p*t*r;
    printf("Simple Intrest: ", SI);
    return 0;
}